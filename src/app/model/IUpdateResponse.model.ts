
/**
 * @description This interface represent the model structure of JSON response after Update of Meme is successful or any CRUD operation fails
 * @property {string} message Represents the response message
 */
export interface IUpdateResponse {
    message: string;
}